---
dependency:
  name: galaxy

driver:
  name: podman

platforms:
  - name: instance
    image: docker.io/pycontribs/centos:8
    pre_build_image: true
    privileged: true
    command: /sbin/init

provisioner:
  name: ansible
  options:
    diff: true

verifier:
  name: ansible
